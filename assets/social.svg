<svg xmlns="http://www.w3.org/2000/svg" width="1280" height="640" viewBox="0 0 1280 640"
     font-family="Helvetica Neue, Helvetica, Arial, sans-serif">
  <defs>
    <radialGradient id="glow" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#3987e5" stop-opacity=".22"/>
      <stop offset="100%" stop-color="#3987e5" stop-opacity="0"/>
    </radialGradient>
  </defs>

  <rect width="1280" height="640" fill="#14140f"/>
  <circle cx="640" cy="320" r="330" fill="url(#glow)"/>

  <!-- Wordmark alone. The baseline sits above the geometric centre so the block
       reads centred despite the descenders of p and y. -->
  <text x="640" y="366" text-anchor="middle" font-size="180" font-weight="700"
        letter-spacing="-6" fill="#ffffff">pyokf</text>
</svg>
